---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

// --- Data & Config ---
const stats = [
  { label: 'دقت', value: '100%', icon: 'target' },
  { label: 'کاربران فعال', value: '+10', icon: 'users' },
  { label: 'سرعت', value: '< 10ms', icon: 'zap' },
  { label: 'هزینه', value: 'رایگان', icon: 'tag' }
];



// --- Icon Paths (Clean Architecture) ---
const icons = {
  chart: "M3 13.2V21h18v-7.8M3 13.2l7.1-7.1a2 2 0 0 1 2.8 0l2.2 2.2a2 2 0 0 0 2.8 0L21 5.2",
  calc: "M9 7h6m0 4h.01M9 11h.01M15 11h.01M9 15h.01M12 15h.01M15 15h.01M6 21h12a3 3 0 0 0 3-3V6a3 3 0 0 0-3-3H6a3 3 0 0 0-3 3v12a3 3 0 0 0 3 3Z",
  shield: "M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z",
  moon: "M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z",
  offline: "M18.36 6.64a9 9 0 1 1-12.73 0M12 2v10",
  arrowRight: "M5 12h14M12 5l7 7-7 7",
  target: "M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 16a6 6 0 1 1 6-6 6 6 0 0 1-6 6z M12 8a4 4 0 1 0 4 4 4 4 0 0 0-4-4z",
  users: "M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2 M23 21v-2a4 4 0 0 0-3-3.87 M16 3.13a4 4 0 0 1 0 7.75",
  zap: "M13 2L3 14h9l-1 8 10-12h-9l1-8z",
  tag: "M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z M7 7h.01"
};
---

<Layout title="Shatoor - Professional Gold Analytics">
  <Navbar currentPage="home" />
  
  <main class="overflow-x-hidden selection:bg-gold-500/30">
    
    <section class="relative min-h-[95vh] flex items-center justify-center pt-24 pb-12 overflow-hidden bg-white dark:bg-gray-950">
      
      <div class="absolute inset-0 z-0">
        <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808008_1px,transparent_1px),linear-gradient(to_bottom,#80808008_1px,transparent_1px)] bg-[size:32px_32px] [mask-image:radial-gradient(ellipse_60%_50%_at_50%_0%,#000_70%,transparent_100%)]"></div>
        <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[400px] bg-gold-500/10 blur-[120px] rounded-full pointer-events-none"></div>
      </div>

      <div class="container mx-auto px-4 relative z-10">
        <div class="flex flex-col items-center text-center max-w-6xl mx-auto">
          
 
      <div class="container mx-auto px-4 relative z-10 text-center">


        <h2 class="text-4xl md:text-6xl font-black mb-8 tracking-tighter leading-none animate-fade-in-up delay-100">
          <span class="text-gray-900 dark:text-white">بدون استرس معامله کن</span><br/>
        </h2>

        <h1 class="text-xl md:text-2xl text-gray-500 dark:text-gray-400 mb-12 max-w-2xl mx-auto leading-relaxed animate-fade-in-up delay-200">
          شاتور,ابزاری جامع برای معاملات طلای شما.
          با شاتور راحتتر متوجه خرید یا فروش بشوید.
        </h1>

        <div class="flex flex-col sm:flex-row items-center justify-center gap-6 animate-fade-in-up delay-300">
          <a href="/calculator" class="group relative px-12 py-6 bg-gray-900 dark:bg-white text-white dark:text-gray-900 rounded-2xl font-black text-xl overflow-hidden transition-transform hover:scale-105 shadow-2xl">
            <div class="absolute inset-0 w-full h-full bg-gradient-to-r from-transparent via-white/10 to-transparent -translate-x-full group-hover:animate-shimmer"></div>
            <span class="relative flex items-center gap-3">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="3" viewBox="0 0 24 24"><path d={icons.arrowRight} /></svg>

              محاسبه حباب طلا 
            </span>
          </a>
          <a href="#features" class="px-12 py-6 border-2 border-gray-200 dark:border-gray-800 text-gray-900 dark:text-white rounded-2xl font-bold text-xl hover:bg-gray-50 dark:hover:bg-gray-900 transition-all">
            ابزار ها
          </a>
        </div>

<div class="grid grid-cols-2 md:grid-cols-4 gap-6 mt-20 w-full max-w-5xl mx-auto animate-fade-in-up delay-500">             {stats.map(stat => (
                <div class="p-6 rounded-3xl bg-white/60 dark:bg-gray-900/60 backdrop-blur-xl border border-gray-100 dark:border-gray-800 flex flex-col items-center gap-2 group hover:border-gold-500/30 transition-all duration-300 hover:-translate-y-1 shadow-lg shadow-gray-200/50 dark:shadow-black/20">
                    <div class="p-3 bg-gray-100 dark:bg-gray-800 rounded-xl text-gold-600 dark:text-gold-400 group-hover:scale-110 transition-transform">
                      <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d={icons[stat.icon]} /></svg>
                    </div>
                    <span class="text-3xl font-bold text-gray-900 dark:text-white tracking-tight">{stat.value}</span>
                    <span class="text-sm font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide">{stat.label}</span>
                </div>
             ))}
          </div>
        </div>
      </div>
    </section>


    <section id="features" class="py-32 bg-white dark:bg-gray-950 relative">
      <div class="container mx-auto px-4">
        <div class="flex flex-col md:flex-row justify-between items-end mb-16 gap-6">
          <div class="max-w-2xl">
            <h2 class="text-4xl md:text-6xl font-bold text-gray-900 dark:text-white mb-6 tracking-tight">همه چیز در یک جا .<br/><span class="text-gray-400 dark:text-gray-600">با چند کلیک دیگه ضرر نکن.</span></h2>
            <p class="text-lg text-gray-600 dark:text-gray-400">همه چیز هایی که شما نیاز دارید در یک داشبورد حرفه ای و آفلاین با دقت و سرعت بالا.</p>
          </div>
          <a href="/calculator" class="hidden md:flex items-center gap-2 font-bold text-gold-600 dark:text-gold-400 hover:gap-4 transition-all">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d={icons.arrowRight}/></svg>

              تمام ابزار ها
          </a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-6 h-auto md:h-[600px]">
          
          <div class="md:col-span-2 lg:col-span-2 row-span-2 relative rounded-[2.5rem] p-10 bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-800 overflow-hidden group hover:border-gold-500/30 transition-all duration-500">
            <div class="relative z-10 h-full flex flex-col justify-between">
              <div>
                <div class="w-16 h-16 bg-white dark:bg-gray-800 rounded-2xl flex items-center justify-center mb-8 shadow-sm group-hover:scale-110 transition-transform duration-300">
                  <svg class="w-8 h-8 text-gold-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d={icons.calc} /></svg>
                </div>
                <h3 class="text-3xl font-bold text-gray-900 dark:text-white mb-3">محاسبه گر حباب.</h3>
                <p class="text-gray-500 dark:text-gray-400 text-lg leading-relaxed max-w-sm">
                  قیمت ذاتی طلا رو بدونید تا متوجه بشید باید بخرید یا بفروشید.
                </p>
              </div>
              
              <div class="mt-8 w-full h-48 bg-white dark:bg-gray-800 rounded-2xl border border-gray-100 dark:border-gray-700 shadow-xl p-4 flex flex-col gap-3 opacity-80 group-hover:opacity-100 group-hover:translate-y-[-10px] transition-all duration-500">
                  <div class="flex justify-between items-center pb-2 border-b border-gray-100 dark:border-gray-700">
                      <div class="w-20 h-2 bg-gray-200 dark:bg-gray-600 rounded-full"></div>
                      <div class="w-8 h-2 bg-green-400 rounded-full"></div>
                  </div>
                  <div class="flex-1 flex items-center justify-center">
                      <span class="text-4xl font-mono font-bold text-gray-900 dark:text-white">12.5%</span>
                  </div>
                  <div class="grid grid-cols-4 gap-2">
                      {[1,2,3,4].map(i => <div class="h-8 bg-gray-100 dark:bg-gray-700 rounded-lg"></div>)}
                  </div>
              </div>
            </div>
            
            <div class="absolute -right-20 -bottom-20 w-80 h-80 bg-gold-500/10 blur-[80px] rounded-full group-hover:bg-gold-500/20 transition-all duration-500"></div>
          </div>

          <div class="md:col-span-1 lg:col-span-1 row-span-2 relative rounded-[2.5rem] p-8 bg-gray-900 dark:bg-black text-white border border-gray-800 dark:border-gray-800 overflow-hidden group">
            <div class="relative z-10 h-full flex flex-col">
              <div class="w-14 h-14 bg-white/10 rounded-2xl flex items-center justify-center mb-6 backdrop-blur-md">
                 <svg class="w-7 h-7 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d={icons.chart} /></svg>
              </div>
              <h3 class="text-2xl font-bold mb-2">محاسبه گر سود و زیان</h3>
              <p class="text-gray-400 text-sm mb-8">محاسبه دقیق سود و زیان شما.</p>
              
              <div class="mt-auto relative h-40 w-full">
                <div class="absolute bottom-0 left-0 right-0 h-full flex items-end justify-between gap-2">
                    {[30, 50, 45, 70, 60, 90, 85].map((h, i) => (
                        <div class="w-full bg-gold-500/20 rounded-t-sm relative group-hover:bg-gold-500/40 transition-colors duration-500" style={`height: ${h}%`}>
                            {i === 5 && <div class="absolute -top-8 left-1/2 -translate-x-1/2 bg-white text-black text-xs font-bold px-2 py-1 rounded shadow-lg">+24%</div>}
                        </div>
                    ))}
                </div>
              </div>
            </div>
          </div>

          <div class="relative rounded-[2.5rem] p-8 bg-blue-50 dark:bg-blue-900/10 border border-blue-100 dark:border-blue-900/30 group hover:shadow-lg transition-all duration-300">
             <div class="flex justify-between items-start mb-4">
                 <div class="p-3 bg-blue-100 dark:bg-blue-900/40 rounded-xl text-blue-600 dark:text-blue-400">
                    <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d={icons.offline} /></svg>
                 </div>
             </div>
             <h3 class="text-xl font-bold text-gray-900 dark:text-white">قابلیت آفلاین</h3>
             <p class="text-sm text-gray-500 mt-2">بدون نیاز به اینترنت استفاده کنید.</p>
          </div>

          <div class="relative rounded-[2.5rem] p-8 bg-purple-50 dark:bg-purple-900/10 border border-purple-100 dark:border-purple-900/30 group hover:shadow-lg transition-all duration-300">
             <div class="flex justify-between items-start mb-4">
                 <div class="p-3 bg-purple-100 dark:bg-purple-900/40 rounded-xl text-purple-600 dark:text-purple-400">
                    <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d={icons.moon} /></svg>
                 </div>
             </div>
             <h3 class="text-xl font-bold text-gray-900 dark:text-white">حالت تاریک</h3>
             <p class="text-sm text-gray-500 mt-2">راحت بودن چشم در هنگام استفاده.</p>
          </div>

        </div>
      </div>
    </section>

    <section class="py-24  border-t border-gray-100 dark:border-gray-800">
        <div class="container mx-auto px-4">
            <div class="max-w-4xl mx-auto">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-12 relative">
                    <div class="hidden md:block absolute top-12 left-0 w-full h-0.5 border-t-2 border-dashed border-gray-800 dark:border-gray-300 -z-10 "></div>
                    
                    {[
                        { step: '01', title: 'نوع ابزار رو انتخاب کنید.', desc: 'از ابزار های شاتور هر کدام که لازم داشتید را انتخاب کنید' },
                        { step: '02', title: 'وارد کردن', desc: 'مقدار های خواسته شده را وارد کنید.' },
                        { step: '03', title: 'نتیجه را مشاهده کنید', desc: 'خیلی سریع و راحت نتیجه را مشاهده کنید.' }
                    ].map((item, index) => (
                        <div class="flex flex-col items-center text-center group">
                            <div class="w-24 h-24 bg-gray-800 dark:bg-gray-200 border-2 border-gray-100 dark:border-gray-800 rounded-full flex items-center justify-center text-2xl font-black text-gray-300 dark:text-gray-700 mb-6 group-hover:border-gold-500 group-hover:text-gold-500 transition-all duration-300 shadow-lg">
                                {item.step}
                            </div>
                            <h3 class="text-xl font-bold text-gray-900 dark:text-gray-100 mb-2">{item.title}</h3>
                            <p class="text-gray-500 dark:text-gray-200/50 text-sm leading-relaxed">{item.desc}</p>
                        </div>
                    ))}
                </div>
            </div>
        </div>
    </section>


    <Footer />

  </main>

  <style>
    /* Custom Animations */
    @keyframes shimmer {
      100% { transform: translateX(100%); }
    }
    
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes marquee {
      0% { transform: translateX(0); }
      100% { transform: translateX(-100%); } /* LTR standard */
    }

    @keyframes gradientText {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .animate-shimmer {
      animation: shimmer 2s infinite;
    }

    .animate-fade-in-up {
      animation: fadeInUp 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
      opacity: 0;
    }

    .animate-marquee {
      animation: marquee 30s linear infinite;
    }
    .animate-marquee2 {
      animation: marquee 30s linear infinite;
      transform: translateX(100%);
    }

    .animate-gradient-text {
      background-size: 200% auto;
      animation: gradientText 5s linear infinite;
    }

    .delay-100 { animation-delay: 0.1s; }
    .delay-200 { animation-delay: 0.2s; }
    .delay-300 { animation-delay: 0.3s; }
    .delay-500 { animation-delay: 0.5s; }
  </style>
</Layout>